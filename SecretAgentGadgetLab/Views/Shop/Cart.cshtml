@model IEnumerable<SecretAgentGadgetLab.Models.Cart>
@{
    ViewData["Title"] = "Cart";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container cart-wrapper">

    <div class="cart-header">
        <span class="cart-header-icon">🛒</span>
        <div>
            <p class="cart-header-label">Mission Loadout</p>
            <h1 class="cart-header-title">Your Cart</h1>
        </div>
    </div>
    <!-- If a card is empty, it displays that the card is empty to a secret agent :) -->
    @if (!Model.Any())
    {
        <div class="cart-empty">
            <span style="font-size: 3rem;">📦</span>
            <p class="mt-3">Your cart is empty, agent.</p>
            <a asp-controller="Shop" asp-action="Index" class="btn btn-primary mt-2">Browse Gadgets</a>
        </div>
    }
    else
    {
    <!-- If it is not empty, it shows all the added gadgets -->
        <div class="cart-table-wrapper">
            <table class="table cart-table">
                <thead>
                    <tr>
                        <th></th>
                        <th>GADGET</th>
                        <th>QTY</th>
                        <th>UNIT PRICE</th>
                        <th>SUBTOTAL</th>
                        <th>DATE</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="cart-item-photo">
                                @if (item.Gadget?.Photo != null)
                                {
                                    <img src="@item.Gadget.Photo" alt="@item.Gadget.Name" class="cart-product-thumb" />
                                }
                                else
                                {
                                    <span class="cart-photo-placeholder">🔧</span>
                                }
                            </td>
                            <td class="cart-gadget-name">@item.Gadget?.Name</td>
                            <td>@item.Quantity</td>
                            <td>@String.Format("${0:N2}", item.Price)</td>
                            <td class="cart-subtotal">@String.Format("${0:N2}", item.Price * item.Quantity)</td>
                            <td class="cart-date">@item.DateCreated.ToString("MMM dd, yyyy")</td>
                            <td>
                                <form asp-action="RemoveFromCart" asp-route-id="@item.Id" method="post" style="display:inline;">
                                    <button type="submit" class="cart-delete-btn">✕ Remove</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="cart-footer">
            <div class="cart-total-box">
                <span class="cart-total-label">▶ TOTAL COST</span>
                <span class="cart-total-value">@String.Format("${0:N2}", Model.Sum(i => i.Price * i.Quantity))</span>
            </div>
            <div class="cart-actions">
                <a asp-controller="Shop" asp-action="Index" class="btn btn-outline-lime">← Continue Shopping</a>
                <a asp-controller="Shop" asp-action="Checkout" class="btn btn-outline-lime">
                    Proceed to Checkout →
                </a>
            </div>
        </div>
    }

</div>
